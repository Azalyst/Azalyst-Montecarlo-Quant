__pycache__/
*.pyc
.claude/
.venv/
venv/
.env
.DS_Store

# NOTE: state/ and docs/ are intentionally tracked — the GitHub Action commits
# the live challenge state and dashboard feed back to the repo each tick.
